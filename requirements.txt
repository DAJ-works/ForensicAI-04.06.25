# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.4.0
pandas>=1.3.0

# Video processing
opencv-python>=4.5.0
moviepy>=1.0.3
ffmpeg-python>=0.2.0

# Deep learning
# NOTE: torch/torchvision are installed CPU-only in the Dockerfile (from the
# PyTorch CPU index) to avoid pulling ~3GB of CUDA wheels into a CPU container.
torch>=1.9.0
torchvision>=0.10.0
ultralytics>=8.0.0  # YOLOv8 - required by object_detector and weapon_detector

# Web API
fastapi>=0.68.0
uvicorn>=0.15.0
python-multipart>=0.0.5
aiofiles>=0.7.0
flask>=2.0.0
flask-cors>=3.0.0
python-dotenv>=1.0.0
gunicorn>=21.2.0  # Production WSGI server (replaces the Flask dev server)
requests>=2.26.0  # Used by backend/api/app.py
huggingface-hub>=0.20.0  # InferenceClient for the chat feature

# Database
sqlalchemy>=1.4.0
psycopg2-binary>=2.9.0
pymongo>=3.12.0  # Optional - use if you choose MongoDB

# Utilities
tqdm>=4.61.0
pillow>=8.3.0
scikit-learn>=0.24.0
scikit-image>=0.18.0